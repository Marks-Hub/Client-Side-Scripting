class framesClass {

	// CONSTRUCTOR -- used to instantiate objects pf the HorrorMovie class
	constructor(aDesc, aCode, aFactor) {
		// the order of the arguments/parameters (the signature) matters
		this.Desc = aDesc;
		this.Code = aCode;
		this.Factor = aFactor;
	}

	// classes can have methods


}